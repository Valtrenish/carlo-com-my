

## Plan: Update favicon to Carlo blue icon

### Changes

1. **Copy uploaded file** `user-uploads://FavIcon_Blue_32x32.png` → `public/favicon.png`

2. **`index.html`** — Add `<link rel="icon" type="image/png" href="/favicon.png">` in the `<head>` section (before the `<title>` tag)

